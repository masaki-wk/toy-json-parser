# json_checker

This directory contains files of the test suite from [JSON_checker](https://json.org/JSON_checker/).

These files are used by `../test.rs` to validate the JSON lexer and parser implementation.
